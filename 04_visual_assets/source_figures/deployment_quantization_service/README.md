# Source-derived deployment and service figure gallery

These 26 SVGs are direct copies of the physically present files in `dcgan_lab/results/figures/`, renamed with readable ASCII filenames.

The group contains:

- ONNX/BN fusion and multi-engine latency/throughput figures;
- PTQ, layer sensitivity, mixed precision, and QAT figures;
- staged service concurrency/resource figures;
- four soak-labeled figures that are retained as source-folder provenance only.

The current public service claim is based on the structured staged run archived under `03_metrics_and_logs/deployment_optimization/06_Service_Stress/`. The four soak-labeled SVGs are not used to claim that the latest staged run completed a long soak, because the corresponding current result folder is empty in the scanned workspace.
