# Metrics and logs

This directory contains copied JSON metrics and CSV training logs from the selected historical result directories. The files are evidence artifacts, not a guarantee that every script is directly runnable outside Kaggle.

The headline FID field should be read together with `metric_protocol.md` at the snapshot root.
